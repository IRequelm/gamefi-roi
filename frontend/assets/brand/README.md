# GamCryp Brand Assets

The official, unaltered GamCryp logo image is stored here:

```text
frontend/assets/brand/gamcryp-logo.png
```

Do not redraw, trace, crop, or modify the logo. The public beta header loads this source asset directly and uses CSS sizing that preserves its aspect ratio.
